/**
 * JSON stringify replacer to conver Maps to json
 */
export function jsonReplacer(_, value) {
  if (value instanceof Map) {
    return {
      dataType: 'Map',
      value: Array.from(value.entries()), // or with spread: value: [...value]
    };
  } else {
    return value;
  }
}
